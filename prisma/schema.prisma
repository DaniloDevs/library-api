generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Book {
  id       String  @id @default(uuid())
  name     String  @unique
  author   String
  category String
  reserved Boolean 

  @@map("books")
}
